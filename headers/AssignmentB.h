// include pre-processor directive
#include <stdio.h>
#include <math.h>
// define MACRO expression to determine if
// the loop should repeat
# define conditionFactorial(i) (i = 1; i <= n; i++)

// declare functions
int genFactorial (int);
int genFactorialReference(int);